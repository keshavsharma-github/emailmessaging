package com.keshav.emailmessaging.config;

//import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//import springfox.documentation.builders.ApiInfoBuilder;
//import springfox.documentation.builders.PathSelectors;
//import springfox.documentation.builders.RequestHandlerSelectors;
//import springfox.documentation.service.ApiInfo;
//import springfox.documentation.service.Contact;
//import springfox.documentation.spi.DocumentationType;
//import springfox.documentation.spring.web.plugins.Docket;

@Configuration
public class SwaggerConfig {
	/*
	 * 
	 * @Bean public Docket api() { return new
	 * Docket(DocumentationType.SWAGGER_2).select()
	 * .apis(RequestHandlerSelectors.basePackage(
	 * "com.keshav.emailmessaging.controller"))
	 * .paths(PathSelectors.any()).build().apiInfo(apiInfo()); }
	 * 
	 * private ApiInfo apiInfo() { return new
	 * ApiInfoBuilder().title("Upload and Send Email API") .description(
	 * "Upload Excel with details then API will extract and Send Email using the details provided"
	 * ) .version("1.0.0").contact(new Contact("Keshav Sharma",
	 * "Keshav Sharma Website", "Keshav Sharma Email")) .build(); }
	 */}
